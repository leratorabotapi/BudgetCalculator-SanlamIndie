import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import Icon from './'

<Meta title="BudgetCalculator/Icon" component={Icon} />

# Default:

<Canvas>
    <Story name="default">
        <Icon name="home" />
    </Story>
</Canvas>

# these are other icon options:

<Canvas>
    <Story name="others">
       <Icon name="reports" />
       <Icon name="transactions" />
       <Icon name="budget" />
       <Icon name="search" />
       <Icon name="settings" />
    </Story>
    
</Canvas>